#include <bits/stdc++.h> // header file includes every Standard library
using namespace std;

int main() {
    int N, M, ans = 0;
    cin >> N >> M;

    if (N > 1) {
        ans += N * (N - 1) / 2;
    }

    if (M > 1) {
        ans += M * (M - 1) / 2;
    }

    cout << ans;

    return 0;
}
