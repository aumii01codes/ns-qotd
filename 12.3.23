#include <bits/stdc++.h> // header file includes every Standard library
using namespace std;
int main() {
	int n;
    cin>>n;
    string s;
    cin>>s;
    if (s[n-1]=='o') cout<<"Yes";
    else cout<<"No";
    return 0;
}
