#include <bits/stdc++.h> // header file includes every Standard library
using namespace std;
int main() {
	float a, b;
  cin>>a>>b;
  double kcal= (a*b)/100;
  cout<<fixed<<setprecision(2)<<kcal;
  return 0;
}
