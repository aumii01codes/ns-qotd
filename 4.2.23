#include <bits/stdc++.h> // header file includes every Standard library
using namespace std;

int main() {
	int n;
    cin>>n;
    int a[n];
    int i, sum=0;
    
    for (i=0; i<n; i++) {
    	cin>>a[i];
    	
    	if (a[i]>10) {
    		sum += (a[i]-10);
		}
	}
    
    cout<<sum;
    return 0;
}
