#include <bits/stdc++.h> // header file includes every Standard library
using namespace std;

int main() {
	int i, j, n;
	cin>>n;
	
	for (i=1; i<=9; i++) {
		for (j=1; j<=9; j++){
			if (i*j==n) {
				cout<<"Yes";
				return 0;
			}
		}
	}
	
	cout<<"No";
    return 0;
}
