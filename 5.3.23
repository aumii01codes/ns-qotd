#include <bits/stdc++.h> // header file includes every Standard library
using namespace std;

int main() {
	int a, b, i;
    cin>>a>>b;
    int count=0;
    for (i=a; i<=b; i++) count++;
    cout<<count;
    return 0;
}
