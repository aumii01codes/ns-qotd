#include <bits/stdc++.h> // header file includes every Standard library
using namespace std;
int main() {
	  int d, t, s;
    cin>>d>>t>>s;
    int ns=d/t;
    if (s>=ns) cout<<"Yes";
    else cout<<"No";
    return 0;
}
